server:
  port: 8081

spring:
  config:
    import: configserver:http://localhost:8085
  application:
    name: USER-SERVICE
  datasource:
    url: jdbc:mysql://localhost:3306/microservices
    username: root
    password: root@123


  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true